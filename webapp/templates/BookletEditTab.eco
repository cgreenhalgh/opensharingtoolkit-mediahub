<%# Thing add or edit tab, takes { data: Thing, add: true|false } as data (title, description, ...); parent is a content %>
    <label>Content
      <textarea name="htmlcontent"><%= @content %></textarea>
    </label>

